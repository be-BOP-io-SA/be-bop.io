export interface PspBtcpayTranslations {
  hero: {
    eyebrow: string;
    title: string;
    subtitle: string;
    btnYoutube: string;
    btnPeertube: string;
  };
  requirement: string; // HTML — contains <strong>
  note: string; // HTML
  accordion: {
    title: string;
    steps: {
      title: string;
      body: string; // HTML
    }[];
  };
}
