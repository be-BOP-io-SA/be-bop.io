export interface ReportTenantTranslations {
  hero: {
    eyebrow: string;
    title: string;
    subtitle: string;
  };
  guide: {
    title: string;
    intro: string;
    cards: {
      counts: { title: string; desc: string };
      include: { title: string; desc: string };
      next: { title: string; desc: string };
    };
  };
  form: {
    title: string;
    note: string;
  };
}
