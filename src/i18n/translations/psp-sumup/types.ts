export interface PspSumupTranslations {
  hero: {
    eyebrow: string;
    title: string;
    subtitle: string;
  };
  accordion: {
    title: string;
    steps: {
      title: string;
      body: string; // HTML
    }[];
  };
}
